<Window x:Class="WpfApplication3.FindDataTemplateControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApplication3"
        xmlns:lib ="clr-namespace:ClassLibrary1;assembly=ClassLibrary1"
        Title="FindDataTemplateControl" Height="350" Width="525">
    <Window.Resources>
        <lib:Student x:Key="stu" Id="1"  Name="Hyman" Skill="Linux" HasJob="True"/>
        <DataTemplate x:Key="stuDT">
            <StackPanel Orientation="Horizontal">
                <TextBox  Name="textbox1" Text="{Binding Id}"/>
                <TextBox  Name="textbox2" Text="{Binding Name}"/>
                <TextBox  Name="textbox3" Text="{Binding Skill}"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <ContentPresenter x:Name="cp" Content="{StaticResource stu}" ContentTemplate="{StaticResource stuDT}" />
            <Button x:Name="button" Width="120" Height="30"  Click="button_Click" Content="find"/>
            <ContentControl>
                
            </ContentControl>
        </StackPanel>
    </Grid>
</Window>